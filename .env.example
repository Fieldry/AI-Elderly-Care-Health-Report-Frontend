VITE_API_BASE_URL=/api

# Optional: when not using the Vite proxy, point requests to the backend directly.
# VITE_BACKEND_ORIGIN=http://127.0.0.1:8001

# Optional: explicit speech-to-text websocket URL.
# VITE_STT_WS_URL=ws://127.0.0.1:8001/ws/stt
