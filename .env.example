# API 基础 URL
# 开发环境：通过 Vite 代理转发到后端（http://localhost:8000）
VITE_API_BASE_URL=/api

# 如果需要直接连接后端（不使用代理），可以设置为：
# VITE_API_BASE_URL=http://localhost:8000

# 生产环境示例：
# VITE_API_BASE_URL=https://your-api-domain.com
